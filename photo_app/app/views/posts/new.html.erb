<%= form_for @post, :html => { :multipart => true} do |f| %>
<p>
	<%= f.label :caption %></br>
	<%= f.text_area :caption, :rows => 10, :cols => 50 %>
</p>
<p>
	<%= f.file_field :photo %>
</p>
<p><%= f.submit "Submit" %></p>
<%end%>